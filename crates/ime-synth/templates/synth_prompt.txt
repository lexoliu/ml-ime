你是中文输入法训练语料的编写者。下面给你一个中文互联网上的流行词，以及它的准确释义。请**只依据这份释义**来使用这个词，不要依赖你自己对它的印象；如果释义和你的印象不一致，以释义为准。

词：{{ term }}
释义：{{ explanation }}

请写出 {{ count }} 条聊天记录片段。每一条都是一次真实的输入法输入场景：text 是这个人此刻正要打出来的那句话，context 是紧挨在它前面的一句话（对方说的，或者自己上一句）。

要求：
1. text 必须原封不动地包含「{{ term }}」这个词，不要拆开，不要改字。
2. text 是有人真的会打出来的口语句子，不是词典解释。禁止出现「意思是」「指的是」「形容」「用来表示」这类解释性说法。
3. text 长度在 {{ minimum }} 到 {{ maximum }} 个字之间，只用简体汉字和中文标点，不要出现英文字母、阿拉伯数字、颜文字、表情符号、井号话题、@ 提及。
4. 标点要省着用：每写一个标点符号，同一句里至少要有 {{ han_per_mark }} 个汉字。想加标点就把句子写长一点。
5. context 用同样的字符限制，长度不超过 {{ maximum }} 个字；如果这句话是自己先开口，context 写成空字符串。
6. {{ count }} 条之间的场景、语气和句式要互不相同，不要只换几个字。
7. 输出严格的 JSON 数组，不要写解释，不要用代码块围栏：
[{"context": "<上一句>", "text": "<正要打出来的句子>"}]
8. 数组长度必须正好是 {{ count }}。
